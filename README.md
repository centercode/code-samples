# Code Samples

Providing code samples for quickly reuse. catalog as follow:

```
$ tree -L 2 -I README.md
.
├── bash-samples
├── java-samples
│   ├── jdk-sample
│   └── simple-web-server
└── maven-samples
    ├── maven-archetype
    └── maven-plugin
```

